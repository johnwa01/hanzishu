

class Level {
  int id;
  int numberOfLessons;
  bool isCompleted;

  Level(int id,
    int numberOfLessons,
    bool isCompleted) {
    this.id = id;
    this.numberOfLessons = numberOfLessons;
    this.isCompleted = isCompleted;
  }
}